{
  "name": "cojourney",
  "description": "We're going to make it. Together.",
  "version": "0.0.1",
  "private": true,
  "author": "Cojourney (https://cojourney.app)",
  "repository": "https://github.com/cojourneyai/cojourney",
  "bugs": "https://github.com/cojourneyai/cojourney/issues",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**electron**"
    ]
  },
  "resolutions": {
    "@types/react": "^18.2.0"
  },
  "scripts": {
    "dev": "lerna run dev --scope @cojourney/agent --scope @cojourney/app --scope @cojourney/worker",
    "dev:all": "lerna run dev",
    "cj": "lerna run build --scope @cojourney/agent --scope @cojourney/shell && cd packages/shell && npm run dev",
    "build": "lerna run build",
    "test": "lerna run test",
    "coverage": "lerna run coverage",
    "types": "lerna run types",
    "lint": "lerna run lintt",
    "format": "lerna run format"
  },
  "devDependencies": {
    "lerna": "^8.1.2",
    "vite-plugin-cachedir": "^1.1.1"
  }
}
