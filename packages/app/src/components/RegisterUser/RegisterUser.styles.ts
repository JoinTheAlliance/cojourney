import { createStyles } from "@mantine/styles";

const useRegisterUserStyles = createStyles(() => ({
  content: {
    maxWidth: 800,
    margin: "auto",
    padding: 20,
  },
}));

export default useRegisterUserStyles;
